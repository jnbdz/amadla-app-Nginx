---
version: 1
module:
  version: 1.0 # Based on Git tags
  description: NGINX is a http/s server that makes it easy to have a reverse proxy
  authors:
    - Jean-Nicolas Boulay (JN) <https://jean-nicolas.name>
  category: Infrastructure
  tags:
    - server
    - http
    - proxy
  configuration:
    network:
      private:
        ports:
          protocol: tcp
          internal: 8080
          external: 8080
      public:
        ports:
          protocol: tcp
          internal: 8080
          external: 80